---
name: start-end-camera-jump-ko
description: Analyze successful start/end-frame camera-jump video prompts and adapt their frozen-scene, snap-and-hold structure to new reference images. Use for 스타트·엔드 이미지 영상 프롬프트, Google Earth식 점프 이동, 정지 장면 카메라 스냅. Supports analysis-only requests without writing a new prompt.
metadata:
  version: 1.0.0
---

# 스타트·엔드 카메라 점프 프롬프트

성공 사례의 재사용 가능한 구조를 보존하면서 새 이미지와 사용자의 움직임에 맞는 영상 프롬프트를 작성한다. 결과 영상 생성이나 업로드는 이 스킬의 기본 작업이 아니다. 사용자 설명을 우선하고 요청하지 않은 컷, 사물, 형태 변형, 카메라 연출을 추가하지 않는다.

## 작업 모드

- **분석만 요청:** 원문 구절을 근거로 움직임, 카메라/피사체 구분, 방향·경로·속도·순서·도착, 일관성 지시를 분석한다. 마지막에 코어 원칙 / 유지할 프롬프트 구조 / 새 작업마다 필요한 정보를 정리한다. 새 프롬프트를 쓰지 않는다.
- **새 이미지에 적용:** 아래 절차로 구도를 확인하고 최종 프롬프트를 작성한다.
- 이 대화에서 유래한 성공 기준은 [references/success-patterns.md](references/success-patterns.md)를 읽는다. 새로 제공된 성공 원문이 있으면 그것을 우선하며, 서로 다른 사례를 임의로 합치지 않는다.

## 입력과 관찰

1. 이미지 1=스타트, 이미지 2=엔드라는 역할을 확인한다. 다른 매핑을 사용자가 지정하면 따른다. 실제 첨부 또는 접근 가능한 파일을 열어 관찰한다. 없는 이미지를 봤다고 말하지 않는다.
2. 이미지별 카메라 높이의 단서, 도로/통로 축, 소실점, 좌우 배치, 대상 크기, 공통 지형지물, 가려짐을 비교한다. 관찰 사실과 공간 관계에 대한 추정을 구분한다. 정확한 거리·고도·회전 각도·숨은 경로는 이미지 두 장만으로 확정하지 않는다.
3. 영상 길이, 원하는 방향/경로, 점프 횟수, 유지 요소, 금지 변화, 모델 정보를 기존 대화에서 먼저 찾는다. 빈 입력란을 사용자 지시로 해석하지 않는다.
4. 누락 정보가 움직임을 크게 바꾸면 필요한 질문만 묶어서 한다. 이미지가 없으면 첨부를 요청하고 최종 프롬프트를 보류한다. 경로·점프 횟수·영상 길이를 임의로 정하지 않는다. 모델만 없고 동작이 충분히 명확하면 모델 공통 자연어로 작성할 수 있다. 불필요한 전체 설문을 반복하지 않는다.

## 움직임 설계

- 먼저 사용자의 동작을 짧게 해석한다. **카메라 위치 이동 / 회전 / 줌 / 피사체 자체 동작**을 분리한다.
- 이 성공 패턴의 기본은 정지된 장면, 카메라의 불연속 이동, 즉시 정지, 도착 구도 유지이다. 사용자가 피사체 동작이나 다른 방식을 명시하면 그 지시를 우선하고 정지 규칙을 해당 범위에서 수정한다.
- 스냅 전진과 줌을 혼동하지 않는다. 광학적 줌에는 실제 접근이나 고도 하강을 끼워 넣지 않는다. 줌, 하강, 회전은 요청된 경우에만 포함한다.
- 피사체의 자세·세계 내 위치 정지와 화면 픽셀 좌표 고정을 구분한다. 카메라 이동에 따른 원근·시차·화면 크기 변화는 허용하되 자체 애니메이션과 형태 변형은 금지한다. 이동하면서 “does not move one pixel”을 사용하지 않는다.
- 경로는 사용자가 지정한 공간적 연결을 따른다. 두 이미지가 직진만으로 연결되지 않으면 임의의 회전, 궤도 이동, 컷, 모핑으로 해결하지 말고 필요한 연결을 확인한다.
- 각 구간은 정지 → 짧은 motion-blur snap → hard stop → 도착 구도 유지로 표현한다. 블러는 카메라 전환에 속하며 피사체 동작 블러로 쓰지 않는다. 점프 도착점을 보여 주는 방식이며 편집 컷을 추가하지 않는다.
- 정지 시간과 이동 시간을 합쳐 총 길이에 정확히 맞춘다. 0.00–0.80s처럼 소수점 초로 표기한다. 인접 hold/freeze를 이중 계산하지 않는다. 최종 스냅을 끝낸 시점부터 최종 hold를 계산한다.
- 도착 구도는 엔드 이미지의 실제 배치·프레이밍으로 설명한다. 주어지지 않은 거리·대상 점유율·고도를 넣지 않는다. 최종 위치에서 추가 드리프트 없이 멈춘다.

## 문장 구조

다음 순서를 의미적으로 유지하되, 불필요한 항목은 생략한다. 사용자에게 빈칸 템플릿을 최종 프롬프트 대신 주지 않는다.

1. 전체 길이와 “Google Earth navigation jump style — discrete position jumps with hard stops” 같은 이동 방식. 이 스타일을 원하는 작업에만 사용한다.
2. “Everything is a frozen photograph. No object moves. Only camera jumps.”에 해당하는 장면/카메라 역할 규칙. 사용자 동작에 맞게 조정한다.
3. START FRAME: 새 이미지의 구도와 유지 시간.
4. JUMP 구간: 시간, 방향/알려진 거리, 스냅, 완전 정지, 도착 후 보이는 구도. 요청된 횟수만 반복한다.
5. 예외 동작이 요청되었을 때만 해당 위치에 별도 구간을 넣고 허용 범위를 한정한다.
6. END FRAME: 엔드 이미지 구도, complete dead stop, 최종 유지 시간.
7. 동작 횟수 및 금지 사항. 기본 점프 패턴에서는 NO smooth gliding / NO moving objects / NO UI / NO arrows / NO overlays. 예외를 넣었다면 “only”나 “NO smooth” 같은 전역 문장이 예외를 금지하지 않도록 범위를 명시한다.

## 출력

한국어 설명과 영어 복사용 프롬프트를 기본으로 하되 사용자 언어 지정을 따른다.

1. **요청한 움직임 해석:** 관찰에 근거한 시작→도착 연결과 카메라/피사체 구분을 짧게 설명.
2. **최종 프롬프트:** 하나의 코드 블록. 미해결 핵심 정보가 있으면 생성하지 않고 확인 후 제공.
3. **확인 필요:** 실제로 확인할 내용이 있을 때만 표시. 큰 영향을 주는 불확실성을 확정 사실로 쓰지 않는다.

## 최종 점검

- 실제 두 이미지를 확인했는가? 관찰과 추정을 구별했는가?
- 방향, 경로, 카메라 동작과 엔드 구도가 연결되는가?
- 총 시간과 점프 횟수가 맞고, 도착 후 유지 시간이 남는가?
- 피사체 정지와 카메라 이동에 따른 화면 변화가 충돌하지 않는가?
- 사슴, 좌회전 90°, 15/20/300m, 벽돌 건물 등 옛 장면 정보가 근거 없이 남아 있지 않은가?
- 요청하지 않은 동작/컷/대상/형태 변화가 없는가?
- 성공에 대한 설명은 추정으로 표시했는가? 한 번의 성공으로 효과를 보장하지 않았는가?
